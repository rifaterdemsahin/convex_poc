# .env.example — Local environment variables template
# NOTE: Real secrets are never committed to git. Load them at runtime via Azure Key Vault.

# --- AZURE KEY VAULT CONFIGURATION ---
AZURE_KEYVAULT_NAME="dp-kv-deliverypilot"
AZURE_TENANT_ID="your-azure-tenant-id"
AZURE_CLIENT_ID="your-service-principal-client-id"
AZURE_CLIENT_SECRET="your-service-principal-client-secret"

# --- CONVEX CONFIGURATION (stored in Azure Key Vault) ---
# CONVEX_DEPLOY_KEY — Convex deployment key (JSON format)
# CONVEX_URL — Your Convex deployment URL (e.g. https://xxx.convex.cloud)

# --- LOCAL DEVELOPMENT ---
# These are pulled from Key Vault at runtime; set locally for dev only:
# CONVEX_DEPLOY_KEY={"v2":"your-key-here"}
